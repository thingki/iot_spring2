<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="sql_info">
	<select id="selectDatabase" resultType="map" parameterType="map">
	use ${selectDatabase}
	</select>

	<select id="run" resultType="map" parameterType="sql">
		${sqlTa}
	</select>
	
</mapper>